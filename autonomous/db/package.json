{
  "name": "@jadency/autonomous-db",
  "version": "1.0.0",
  "description": "JadencySocial Autonomous System - SQLite Database Layer",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "migrate": "tsx migrate.ts migrate",
    "rollback": "tsx migrate.ts rollback",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
